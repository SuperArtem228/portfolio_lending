# 🚀 Инструкция по деплою

## Вариант 1: Vercel (Рекомендуется) — через веб-интерфейс

1. **Зайдите на [vercel.com](https://vercel.com)** и войдите через GitHub
2. Нажмите **"Add New..."** → **"Project"**
3. Выберите репозиторий `portfolio-landing` из списка
4. Vercel автоматически определит Next.js — просто нажмите **"Deploy"**
5. Через 1-2 минуты ваш сайт будет доступен по адресу:
   - `https://portfolio-landing-xxx.vercel.app`
   - Можно настроить кастомный домен в настройках проекта

**Преимущества:**
- ✅ Автоматический деплой при каждом push в GitHub
- ✅ Бесплатный SSL сертификат
- ✅ CDN по всему миру
- ✅ Preview deployments для каждого PR

---

## Вариант 2: Vercel CLI

### Установка Vercel CLI

```bash
npm install -g vercel
```

### Деплой

```bash
cd /Users/artempopov/Downloads/portfolio-landing
vercel
```

Следуйте инструкциям:
- Логин через браузер (если первый раз)
- Выберите проект (или создайте новый)
- Подтвердите настройки

### Production деплой

```bash
vercel --prod
```

---

## Вариант 3: Netlify

1. Зайдите на [netlify.com](https://netlify.com)
2. **"Add new site"** → **"Import an existing project"**
3. Подключите GitHub репозиторий
4. Настройки сборки:
   - **Build command:** `npm run build`
   - **Publish directory:** `.next`
5. Нажмите **"Deploy site"**

---

## Вариант 4: Другие платформы

### Railway

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Деплой
railway login
railway init
railway up
```

### Render

1. Зайдите на [render.com](https://render.com)
2. **"New"** → **"Web Service"**
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`
5. Нажмите **"Create Web Service"**

---

## После деплоя

1. **Проверьте сайт** — откройте предоставленный URL
2. **Настройте домен** (опционально) — в настройках проекта Vercel/Netlify
3. **Проверьте темы** — переключите Light/Dark режим
4. **Проверьте ссылки** — все контакты должны работать

---

## Автоматический деплой

После первого деплоя через Vercel:
- Каждый push в `main` ветку автоматически деплоится в production
- Каждый PR получает preview URL для тестирования

---

## Troubleshooting

### Ошибка сборки

```bash
# Проверьте локально
npm run build
```

### Проблемы с зависимостями

Убедитесь, что все зависимости в `package.json`:
```bash
npm install
```

### Проблемы с 3D

Если 3D не работает на деплое — это нормально, fallback на градиенты сработает автоматически.


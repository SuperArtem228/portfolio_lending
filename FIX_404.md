# 🔧 Исправление ошибки 404 на Vercel

## Что было исправлено:

1. ✅ Добавлен `vercel.json` для явной конфигурации
2. ✅ Добавлены `engines` в `package.json` для указания версии Node.js
3. ✅ Упрощена конфигурация Next.js

## Шаги для исправления:

### 1. Закоммитьте изменения и запушьте на GitHub:

```bash
cd /Users/artempopov/Downloads/portfolio-landing
git add .
git commit -m "Fix: Add Vercel configuration"
git push
```

### 2. В Vercel Dashboard:

1. Зайдите в настройки проекта (Settings)
2. Перейдите в **"General"** → **"Build & Development Settings"**
3. Убедитесь, что:
   - **Framework Preset:** Next.js
   - **Build Command:** `npm run build` (или оставьте пустым)
   - **Output Directory:** `.next` (или оставьте пустым)
   - **Install Command:** `npm install` (или оставьте пустым)
   - **Node.js Version:** 18.x или 20.x

### 3. Пересоберите проект:

1. В Vercel Dashboard перейдите в **"Deployments"**
2. Найдите последний деплой
3. Нажмите **"..."** → **"Redeploy"**
4. Или просто сделайте новый push в GitHub

### 4. Если всё ещё не работает:

**Вариант A: Удалите и пересоздайте проект на Vercel**

1. Удалите проект в Vercel (Settings → Delete)
2. Создайте новый проект
3. Импортируйте репозиторий заново

**Вариант B: Проверьте структуру на GitHub**

Убедитесь, что на GitHub есть:
- ✅ `app/` папка с `page.tsx` и `layout.tsx`
- ✅ `package.json`
- ✅ `next.config.js`
- ✅ `vercel.json`

**Вариант C: Проверьте логи сборки**

1. В Vercel Dashboard → Deployments
2. Откройте последний деплой
3. Посмотрите **"Build Logs"**
4. Найдите ошибки и сообщите мне

## Проверка локально:

Убедитесь, что проект собирается:

```bash
cd /Users/artempopov/Downloads/portfolio-landing
npm run build
npm start
```

Затем откройте http://localhost:3000 — должно работать.

## Если проблема в путях импорта:

Если Vercel не понимает `@/` алиасы, можно временно заменить на относительные пути, но обычно это не нужно.

---

**После этих шагов проект должен заработать!** 🚀


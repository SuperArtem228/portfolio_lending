# Портфолио-лендинг Артёма Попова

Современный одностраничный портфолио-лендинг в стиле «Золотого Яблока» и нового iOS с glassmorphism, крупными скруглениями и 3D-декором.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Сборка для продакшена

```bash
npm run build
npm start
```

## 📁 Структура проекта

```
portfolio-landing/
├── app/
│   ├── layout.tsx          # Главный layout с SEO и мета-тегами
│   ├── page.tsx            # Главная страница
│   └── globals.css         # Глобальные стили и дизайн-токены
├── components/
│   ├── Header.tsx          # Шапка с навигацией
│   ├── ThemeToggle.tsx     # Переключатель темы
│   ├── Hero.tsx            # Главная секция
│   ├── Metrics.tsx         # Ключевые метрики
│   ├── ExperienceTimeline.tsx  # Опыт работы (таймлайн)
│   ├── Projects.tsx        # Кейсы и проекты
│   ├── Skills.tsx          # Навыки
│   ├── Education.tsx       # Образование и курсы
│   ├── Contact.tsx         # Контакты
│   └── Background3D.tsx    # 3D-декор с fallback
├── data/
│   ├── profile.ts          # Профиль и контакты
│   ├── experience.ts       # Опыт работы
│   ├── projects.ts         # Проекты
│   ├── skills.ts           # Навыки
│   ├── education.ts        # Образование
│   └── metrics.ts          # Ключевые метрики
└── ...
```

## ✏️ Где менять данные

Все данные находятся в папке `/data`:

- **`profile.ts`** — имя, роль, опыт, описание, контакты
- **`experience.ts`** — опыт работы (компании, достижения)
- **`projects.ts`** — кейсы и проекты
- **`skills.ts`** — навыки по категориям
- **`education.ts`** — образование и курсы
- **`metrics.ts`** — ключевые метрики для секции Metrics

### Пример изменения данных

Откройте файл `data/profile.ts`:

```typescript
export const profile = {
  name: 'Ваше Имя',
  role: 'Ваша Роль',
  // ... остальные поля
};
```

## 🎨 Дизайн-система

### Цветовая палитра

Цвета определены через CSS-переменные в `app/globals.css`:

- **Light theme**: молочный фон + тёплые акценты
- **Dark theme**: графит + тёплые градиенты (янтарь/персик/розово-оранжевый)

### Компоненты

- **Glassmorphism**: классы `.glass` и `.glass-strong`
- **Gradient text**: класс `.gradient-text`
- **Скругления**: `rounded-3xl` (24px), `rounded-2xl` (20px)

## 🌐 Деплой на Vercel

1. Загрузите проект в GitHub
2. Зайдите на [vercel.com](https://vercel.com)
3. Импортируйте репозиторий
4. Vercel автоматически определит Next.js и задеплоит

Или через CLI:

```bash
npm i -g vercel
vercel
```

## 🛠 Технологии

- **Next.js 14** (App Router)
- **TypeScript**
- **TailwindCSS** с CSS-переменными
- **Framer Motion** для анимаций
- **React Three Fiber** для 3D-декора (с fallback)

## 📱 Адаптивность

Сайт полностью адаптивен и оптимизирован для:
- Desktop (1920px+)
- Tablet (768px - 1024px)
- Mobile (320px - 767px)

## ♿ Доступность

- Focus states для всех интерактивных элементов
- ARIA-лейблы для кнопок
- Контрастные цвета
- Семантический HTML

## 🎯 Особенности

- ✅ Переключение темы (Light/Dark) с сохранением в localStorage
- ✅ Поддержка `prefers-color-scheme`
- ✅ Smooth scroll навигация
- ✅ 3D-декор с graceful fallback на слабых устройствах
- ✅ SEO оптимизация (meta tags, schema.org)
- ✅ Оптимизация производительности

## 📝 Лицензия

Личный проект. Все права защищены.

